//function that takes array of words and uses join method with space seperator to combine them with a space between and then returns

function smash (words) {
    return words.join(' ')
 };

// the join method combines elements of an array into a single string and the space character inside of the string determines how they are combined