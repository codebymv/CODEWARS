//declare an empty array, reassign the array to the string.split with space character as a seprator

function stringToArray(string){
    let arr = [];
    arr = string.split(' ');
    return arr
    }

